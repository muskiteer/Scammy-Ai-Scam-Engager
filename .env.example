# GUVI Hackathon Callback URL (default: https://hackathon.guvi.in/api/updateHoneyPotFinalResult)
# Leave empty to use default GUVI endpoint, or override with custom URL for testing
# CALLBACK_URL=https://hackathon.guvi.in/api/updateHoneyPotFinalResult

# API Key for authentication (set this to secure your endpoint)
# Clients must send this in x-api-key header
API_KEY=your-secret-api-key-here

# Server Port (default: 8080)
# PORT=8080
